/* =========================
SKELETON LOADING
============================ */

.skeleton {
  position: relative;
  overflow: hidden;
  background: var(--bg-card);
  border-radius: 18px;
}

.skeleton::before {
  content